apply plugin: 'jetty'

jettyRunWar.httpPort = 8080
jettyRunWar.contextPath = ''

project.webAppDirName = 'webapp'

dependencies {
    compile project(':duo-domain')
    compile 'javax.servlet:jstl:1.2'
    providedCompile 'javax.servlet:javax.servlet-api:3.1.0'
}